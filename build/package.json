{
  "name": "enchanted-kingdom-build",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Build tooling for the Enchanted Kingdom web app (emblem render + single-file inline build).",
  "scripts": {
    "icons": "node render_emblem.mjs && node render_icons.mjs && python3 gen_icons.py",
    "build": "python3 build.py",
    "all": "npm run icons && npm run build"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
